# Copy this to .env.local and replace the value with your real Anthropic API key.
# Get one at https://console.anthropic.com
# The key is read at build time and baked into the bundle.
# Never commit your real key to git — .env.local is gitignored.
VITE_ANTHROPIC_API_KEY=sk-ant-...
